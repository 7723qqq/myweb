# FSR3 Mod - Git Flow 标准

## 🎯 分支结构

```
master ──┐         # ✅ 稳定发布版本
    │     release/* ────→ master   (每次发布创建)
    └ develop       # 🔧 日常开发主分支
          ├── feature/*           # 新功能开发
          ├── bugfix/*            # Bug 修复
          └── hotfix/*            # 紧急修复
```

## 📝 提交规范 (Conventional Commits)

| 类型 | 格式 | 示例 |
|------|------|------|
| **feat** | `feat: ...` | 新功能 → `feat: add zero-copy frame buffer` |
| **fix** | `fix: ...` | Bug 修复 → `fix: prevent game crash on load` |
| **docs** | `docs: ...` | 文档更新 → `docs: update FSR3 configuration guide` |
| **chore** | `chore: ...` | 维护任务 → `chore: update mods.toml version to 1.2.0` |

## 🚀 工作流

### 开发新功能
```bash
# 1. 从 develop 创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/new-render-pass

# 2. 开发并提交 (每次小改动都提交)
git add .
git commit -m "feat: implement custom frame queue"

# 3. 完成后合并回 develop
git checkout develop
git merge --no-ff feature/new-render-pass
```

### 发布新版本
```bash
# 1. 从 develop 创建 release
git checkout -b release/4.0.0-fsr3

# 2. 更新版本号和元数据
# (编辑 mods.toml, build.gradle 等)

# 3. 测试通过后合并到 master + tag
git merge --no-ff release/4.0.0-fsr3
git checkout master
git tag -a v4.0.0 -m "Release FSR3 4.0.0"

# 4. 更新 develop (如果做了改动)
git checkout develop
git merge -s ours master
```

## 📊 仓库状态检查

| 命令 | 用途 |
|------|------|
| `git status` | 查看当前状态 |
| `git log --oneline -10` | 最近提交历史 |
| `git diff HEAD~3` | 对比 3 个提交前的改动 |
| `git branch -a` | 所有本地/远程分支 |

## ✨ 自动化优化

- ✅ **每日备份**: 22:00 自动提交变更
- ✅ **pre-commit hook**: 检测 Java 文件和换行符安全
