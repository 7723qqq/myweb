# 开发规则完整记录

## 📅 记录时间
- **记录时间**: 2026-03-11 04:38 GMT+8
- **开发者**: 主人
- **负责人**: 打工人 (AI助理)

## 📁 项目信息
### 项目路径
- **Minecraft 游戏数据**: `C:\Users\34730\Desktop\mc\mc 2\.minecraft\versions\1.20.1-Forge_47.4.16`
- **Mod 源文件**: `C:\Users\34730\Desktop\external\dlss`
- **规则文件目录**: `C:\Users\34730\Desktop\external\dlss\.trae\rules`

### 规则文件列表
根据目录扫描，规则文件包括：
- `0.md` - 基础规则
- `1.md` - 核心原则
- `2.md` - 技术规范
- `3.md` - 实现要求
- `4.md` - 验证方法
- `5.md` - 错误处理
- `6.md` - 资源管理
- `7.md` - 线程安全
- `8.md` - 兼容性
- `9.md` - 开发流程
- `10.md` - 个人规则
- `fsr.md` - FSR 特定规则

## 🔒 个人规则（AI需严格遵守）
### 规则1: 保护隐私
- **说明**: 在代码、配置文件、日志中不得包含任何个人信息
- **具体要求**: 
  - 不得包含姓名、邮箱、电话、地址、QQ、微信等
  - 所有占位符应使用通用术语（如 yourname、example.com）
  - 测试数据使用虚构信息

### 规则2: 原创性
- **说明**: 不得直接复制其他开源项目的代码
- **具体要求**:
  - 可参考思路，但实现必须独立编写
  - 若需借鉴少量片段，必须保留原注释并遵守原许可证
  - 借鉴仅限于少量关键算法，不能是完整模块

### 规则3: 错误应当解决而不是注释
- **说明**: 禁止通过注释掉代码来"修复"问题
- **具体要求**:
  - 遇到错误必须找到根本原因并修复
  - 不能使用 `// TODO`、`// FIXME` 作为长期解决方案
  - 临时解决方案必须有明确的时间限制和修复计划

## 🎯 核心原则（技术红线）
### 原则1: 真实可运行
- **禁止项**: 任何占位符、TODO 注释、模拟实现
- **要求项**: 
  - 所有函数必须有具体实现
  - 不得为空函数或返回固定值
  - 代码必须可直接编译运行

### 原则2: 每步可验证
- **要求项**:
  - 每个功能模块必须有明确的验证方法
  - 验证方法包括：日志输出、可视化效果、性能对比
  - 验证过程必须可重复

### 原则3: 完整错误处理
- **要求项**:
  - 所有 API 调用必须检查返回值
  - 失败时必须输出详细错误信息
  - 错误信息必须包含：错误码、函数名、文件名、行号
  - 必须提供降级机制

### 原则4: 正确资源释放
- **要求项**:
  - 严格遵循 RAII 原则（C++）
  - 销毁顺序必须与创建顺序相反
  - 所有 Vulkan 对象必须有对应的销毁操作

### 原则5: 线程安全保证
- **要求项**:
  - 所有共享数据必须用互斥锁保护
  - FSR3 上下文调用必须加锁
  - 避免死锁和竞态条件

## 🛠️ 技能/原则详细描述
### 1. 真实可运行
- **应用场景**: 在提供代码片段、修复 bug、实现新功能时
- **具体要求**: 
  - 生成的代码必须可直接编译运行
  - 无占位符、TODO 或模拟实现
  - 所有函数必须有具体实现，返回实际值

### 2. 每步可验证
- **应用场景**: 在阶段交付时，协助设计测试用例
- **具体要求**:
  - 每个功能模块都应提供验证方法
  - 验证方法包括：日志输出、可视化效果、性能对比
  - AI 应建议如何验证，并协助编写验证代码

### 3. 完整错误处理
- **应用场景**: 在编写 Vulkan 或 OpenGL 代码时，自动插入错误检查
- **具体要求**:
  - 所有 API 调用必须检查返回值
  - 失败时输出详细错误（错误码、函数名、文件名、行号）
  - 提供降级机制

### 4. 正确资源释放
- **应用场景**: 在创建 Vulkan 对象时，同时生成释放代码
- **具体要求**:
  - 严格遵循 RAII（C++）或 try-finally（Java）
  - 销毁顺序与创建顺序相反
  - 确保所有 vkCreate 有对应的 vkDestroy

### 5. 线程安全保证
- **应用场景**: 在多线程访问处自动添加锁
- **具体要求**:
  - 共享数据（如 FSR3 上下文、Vulkan 设备）必须用互斥锁保护
  - FSR3 上下文调用必须加锁

### 6. 掌握 Vulkan/OpenGL 互操作
- **应用场景**: 协助实现零拷贝路径，调试导入错误
- **具体要求**:
  - 能熟练使用 EXT_memory_object 和 EXT_semaphore
  - 包括创建可导出资源、导入句柄、布局转换、信号量同步

### 7. 掌握 FSR 各版本集成
- **应用场景**: 配置 FSR 上下文，调度不同版本的计算
- **具体要求**:
  - 熟悉 FSR 1.0–4.0 的 API 和数据需求
  - 能根据硬件自动选择合适版本
  - 实现版本回退机制

### 8. 掌握 JNI 开发
- **应用场景**: 实现 JNI 接口层，确保稳定通信
- **具体要求**:
  - 能编写符合命名规范的 JNI 函数
  - 正确处理 Java 与 C++ 之间的数据类型转换（jlong ↔ 指针/句柄）
  - 避免内存泄漏

### 9. 掌握 NVIDIA 兼容性处理
- **应用场景**: 在初始化时检测硬件，配置 FSR 版本和扩展
- **具体要求**:
  - 能根据显卡型号和驱动版本启用对应优化或回退

### 10. 掌握多版本回退机制
- **应用场景**: 在配置变更或硬件不兼容时自动切换
- **具体要求**:
  - 实现从 FSR4 → 3 → 2 → 1 → 原生 的平滑降级
  - 切换时资源正确重建

### 11. 掌握 UI 分离技术
- **应用场景**: 修改 Minecraft 渲染流程，分离 UI 绘制
- **具体要求**:
  - 能设计两个渲染通道：场景（低分辨率）和 UI（高分辨率）
  - 在 FSR 升频后正确合成

## 🎮 适用场景
1. **希望在不破坏模组兼容性的前提下大幅提升 Minecraft 性能的模组开发者**
2. **需要将老游戏（基于 OpenGL）迁移到现代图形 API（Vulkan）并集成后处理效果（如 FSR）的开发者**
3. **研究 OpenGL-Vulkan 互操作、零拷贝共享内存、跨 API 同步的技术爱好者**

## 🧩 技术原理
### 拦截与转换
- 通过 Mixin 在 GameRenderer.render 的 HEAD 和 RETURN 处插入钩子
- 将 Minecraft 的原生 OpenGL 渲染调用实时转换为 Vulkan 命令（基于类似 Zink 的转换层）
- 转换后的 Vulkan 命令在 Vulkan 设备上执行，利用 Vulkan 的低开销特性

### 零拷贝共享内存
- 利用 OpenGL 扩展 GL_EXT_memory_object 及平台相关扩展
- 利用 Vulkan 的 VK_KHR_external_memory 系列扩展
- 创建跨 API 共享的 GPU 内存
- Minecraft 直接渲染到共享纹理（低分辨率）
- Vulkan 计算着色器直接读取该内存执行 FSR
- 结果写入另一共享纹理（高分辨率）
- 全程无 CPU 介入

### 跨 API 同步
- 使用 GL_EXT_semaphore 及平台相关扩展
- 通过信号量（semaphore）精确同步 OpenGL 渲染完成和 Vulkan 计算开始
- 确保渲染与计算流水线并行且无竞争

### FSR 集成
- 在 Vulkan 端编写计算着色器实现 AMD FSR 的 EASU（边缘自适应缩放）和 RCAS（锐化）
- 输入为低分辨率共享纹理，输出为高分辨率共享纹理，可直接用于显示

## 🔄 渲染流程
```
Minecraft 渲染 (OpenGL)
↓
MixinGameRenderer 拦截（HEAD）
↓
ZeroCopyFSRRenderer.beginFrame()
├─ 保存 OpenGL 状态
├─ 绑定共享纹理 FBO（低分辨率）
└─ 设置视口为低分辨率
↓
Minecraft 渲染到【共享纹理】← 零拷贝 GPU 内存！
↓
MixinGameRenderer 拦截（RETURN）
↓
ZeroCopyFSRRenderer.endFrame()
├─ 恢复原始渲染目标
├─ processFrameZeroCopy()
│   ├─ OpenGL 发出信号量（渲染完成）
│   ├─ Vulkan 等待信号量
│   ├─ AMD FSR SDK dispatch ← 直接读取共享纹理
│   │   - EASU 边缘自适应缩放
│   │   - RCAS 锐化
│   ├─ Vulkan 发出信号量（处理完成）
│   └─ OpenGL 等待信号量
├─ displayOutputTexture()
│   └─ Blit 到屏幕
└─ 恢复 OpenGL 状态
↓
帧结束
```

## 🛠️ 实现步骤
### 1. 环境准备
- Minecraft 1.20.1 + Forge 47.4.16+
- Java 17+（推荐 21）
- 显卡驱动支持 OpenGL 4.6 及 Vulkan 1.3，且支持以下扩展：
  - OpenGL: GL_EXT_memory_object, GL_EXT_memory_object_fd/win32, GL_EXT_semaphore, GL_EXT_semaphore_fd/win32
  - Vulkan: VK_KHR_external_memory, VK_KHR_external_memory_fd/win32, VK_KHR_external_semaphore, VK_KHR_external_semaphore_fd/win32

### 2. 项目结构
```
src/main/java/com/yourmod/
├── mixin/
│   └── GameRendererMixin.java          # 拦截渲染
├── renderer/
│   ├── ZeroCopyFSRRenderer.java        # 核心渲染器
│   └── FSR3GLVulkanInterop.java        # OpenGL-Vulkan 互操作管理
├── native/
│   ├── vulkan_fsr.cpp                  # Vulkan FSR 实现 + JNI 封装
│   └── CMakeLists.txt                  # 编译 JNI 库
└── FSR3Mod.java                        # 主类
```

## 📋 角色分工
- **开发者**: 主人
- **负责人**: 打工人 (AI助理)
- **工作模式**: 开发者说，负责人改

## 📝 已创建的草案文件
1. **GameRendererMixin.java** - Mixin 拦截器（已创建草案）

---
**记录完成时间**: 2026-03-11 04:39 GMT+8
**文件位置**: `C:\Users\34730\.openclaw-autoclaw\workspace\开发规则完整记录.md`
**下一步**: 根据这些规则开始实现具体模块